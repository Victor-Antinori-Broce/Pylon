-- ═══════════════════════════════════════════════════════════════
-- Auto-loaded by PostgreSQL on first container init
-- (mounted via docker-compose into /docker-entrypoint-initdb.d/)
-- ═══════════════════════════════════════════════════════════════
CREATE EXTENSION IF NOT EXISTS vector;
